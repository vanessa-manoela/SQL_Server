criação de tabela com PK

CREATE TABLE CLIENTE (

CPF VARCHAR (11) NOT NULL,
NOME VARCHAR (100) NULL,
[ENDEREÇO] VARCHAR (150) NULL,
BAIRRO VACHAR (50) NULL,
CIDADE VARCHAR (50) NULL,
ESTADO VARCHAR (50) NULL,
CEP VARCHAR (8) NULL,
[DATA NASCIMENTO] DATE NULL,
IDADE INT NULL,
SEXO VARCHAR (1),
[LIMITE DE CREDITO] FLOAT,
[ VOLUME DE COMPRA] FLOAT,
[PRIMEIRA COMPRA] BIT,

CONSTRAINT PK_CLIENTES PRIMARY KEY CLUSTERED (CPF)
)




CREATE TABLE IF NOT EXISTS ENDERECO
[ID_ENDEREÇO] INT (11) NOT NULL AUTO INCREMENT,
CEP VACHAR (15) NOT NULL,
LOGRADOURO VACHAR (60) NOT NULL,
BAIRRO VARCHAR (60) NOT NULL,
NUMERO VARCHAR (10) NOT NULL,

CONSTRAINT PK_ID_ENDERECO PRIMARY KEY ([ID_ENDEREÇO])
);


Criando tabela com PK, FK e index


CREATE TABLE NOTAS (

NUMERO VARCHAR (5) NOT NULL,
DATA DATE,
CPF VARCHAR (11) NOT NULL,
MATRICULA VACHAR (5) NOT NULL,
IMPOSTO FLOAT,

CONSTRAINT [PK_TABELA DE VENDAS] PRIMARY KEY CLUSTERED (NUMERO)

CONSTRAINT [FK_TABELA CLIENTES] FORENING KEY (CPF)
REFERENCES CLIENTES (CPF),

CONSTRAINT [FK_TABELA VENDEDORES] FORENING KEY (MATRICULA)
REFERENCES BANCO.[TABELA VENDEDORES] (MATRICULA)

);



CREATE TABLE ITENS VENDIDOS(

NUMERO VARCHAR (5) NOT NULL,
[CODIGO] VARCHAR (10) NOT NULL,
QUANTIDADE INT NULL,
[PREÇO] FLOAT,

CONSTRAINT [PK_ITENS VENDIDOS] PRIMARY KEY CLUSTERED 
([NUMERO], [CODIGO]) --chave composta


)


Criando tabela sem FK e adicionar depois

CREATE TABLE IF NOT EXISTS [ENDEREÇO]
ID_ENDERECO INT (11) NOT NULL AUTO INCREMENT,
CEP VARCHAR (15) NOT NULL,
LOGRADOURO VARCHAR (60) NOT NULL,
BAIRRO VARCHAR (60) NOT NULL,
NUMERO VARCHAR (10) NOT NULL,
CONTRAINT PK_ID_ENDERECO PRIMARY KEY ([ID_ENDEREÇO])

ALTER TABLE [NOTAS] ADD COLUMN [ID_CLIENTE_ENDERECO] INT (11) NOT NULL;

ALTER TABLE NOTAS WITH CHECK ADD CONSTRAINT FK_ENDERECO FOREING KEY (ID_CLIENTE_ENDERECO)
REFERENCES BANCO.CLIENTES (CPF) --campo que vai receber, referenciando a tabela e o campo que vem


criação de tabela com Identity
o identity cria campo auto incremento, quando executrado o insert não é necessário informar o campo identity
só pode haver 1 por tabela. É informado na criação da tabela.

CREATE TABLE TBL_IDENTITY (
ID IN IDENTITY (1,1) NOT NULL,
[DESCRIÇÃO] VARCHAR (20) NULL)

INSERT INTO TBL_IDENTITY ([DESCRIÇÃO]) VALUES ('CLIENTE 1')